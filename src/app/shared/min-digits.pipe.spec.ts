import { MinDigitsPipe } from './min-digits.pipe';

describe('MinDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new MinDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
